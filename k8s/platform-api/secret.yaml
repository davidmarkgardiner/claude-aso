apiVersion: v1
kind: Secret
metadata:
  name: platform-api-secrets
  namespace: platform-system
  labels:
    app: platform-api
    component: secrets
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  # JWT_SECRET: base64 encoded JWT secret
  JWT_SECRET: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=  # change-me-in-production # pragma: allowlist secret
  # AZURE_CLIENT_ID: base64 encoded Azure client ID (if needed)
  # AZURE_CLIENT_SECRET: base64 encoded Azure client secret (if needed)
  # AZURE_TENANT_ID: base64 encoded Azure tenant ID (if needed)
  # DB_PASSWORD: base64 encoded database password (if needed)